import 'package:flutter/material.dart';

class PairScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pair Screen'),
      ),
      body: Center(
        child: Text('This is the Pair Screen'),
      ),
    );
  }
}
